import { quiz, questionIds, systemsIds } from './quiz';
import { routes } from './routes';
import { materials } from './material';
import { works } from './works';

export { materials, works, routes, quiz, questionIds, systemsIds };
